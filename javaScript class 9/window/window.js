
//window is parent of all elements
//document is the root of the DOM
//body is the visible part of the HTML document

// window >> document >> body >> html >> head >> title >> h1 >> p >> span >> div >> img